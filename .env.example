# Аналитика. Скопируйте в .env и заполните, когда получите ID.
# Префикс PUBLIC_ обязателен — иначе Astro не отдаёт переменную в браузер.
# Если оставить пустыми — скрипты аналитики не подключаются, ошибок нет.

PUBLIC_YANDEX_METRIKA_ID=
PUBLIC_GA_MEASUREMENT_ID=
