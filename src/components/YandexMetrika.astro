---
// Privacy-friendly Яндекс Метрика (152-ФЗ): счётчик грузится ТОЛЬКО после
// согласия пользователя на аналитические cookies. Выбор хранится в localStorage.
// window.track() is a safe helper for goals (no-op until ym is loaded).
// GA здесь нет намеренно: фактически не используется + трансграничная передача.
const YM_ID = 109657352;
---
<!-- Cookie / analytics consent banner -->
<div class="cc" id="cookieConsent" role="dialog" aria-label="Согласие на использование cookie" hidden>
  <p class="cc__text">
    Используем cookies и Яндекс Метрику для аналитики и улучшения сайта.
    Подробнее в <a href="/privacy">Политике обработки персональных данных</a>.
  </p>
  <div class="cc__actions">
    <button type="button" class="cc__btn cc__btn--accept" id="ccAccept">Принять</button>
    <button type="button" class="cc__btn cc__btn--decline" id="ccDecline">Отклонить</button>
  </div>
</div>

<style>
  .cc {
    position: fixed; right: 16px; bottom: 16px; z-index: 700;
    width: min(520px, calc(100vw - 32px)); padding: 14px;
    display: grid; grid-template-columns: 1fr auto; gap: 14px; align-items: center;
    color: #f3f1ec; background: rgba(17, 17, 17, .97);
    border: 1px solid rgba(243, 241, 236, .18); border-radius: 0;
    font-family: 'Onest', Inter, system-ui, sans-serif;
    animation: cc-in .35s cubic-bezier(.22,1,.36,1);
  }
  @keyframes cc-in { from { opacity: 0; transform: translateY(14px); } to { opacity: 1; transform: none; } }
  .cc[hidden] { display: none; }
  .cc__text { margin: 0; color: #a7a49e; font-size: 11px; line-height: 1.45; }
  .cc__text a { color: #f3f1ec; border-bottom: 1px solid rgba(243, 241, 236, .18); }
  .cc__actions { display: flex; gap: 6px; }
  .cc__btn { min-height: 36px; padding: 0 12px; border: 1px solid rgba(243, 241, 236, .18); border-radius: 0; font-size: 11px; font-weight: 600; cursor: pointer; transition: border-color .2s, color .2s, background .2s; }
  .cc__btn--accept { color: #160a05; background: #ff6534; border-color: #ff6534; }
  .cc__btn--accept:hover { background: #ff7b50; }
  .cc__btn--decline { color: #a7a49e; background: transparent; }
  .cc__btn--decline:hover { color: #f3f1ec; border-color: #f3f1ec; }
  @media (max-width: 620px) { .cc { left: 10px; right: 10px; bottom: 10px; width: auto; grid-template-columns: 1fr; }.cc__actions { width: 100%; }.cc__btn { flex: 1; } }
  @media (prefers-reduced-motion: reduce) { .cc { animation: none; } }
</style>

<script is:inline define:vars={{ YM_ID }}>
  (function () {
    var KEY = 'analyticsConsent'; // 'granted' | 'denied'

    // Safe goal tracker. It also works before Metrika loads.
    window.track = function (name, params) {
      try { if (typeof window.ym === 'function') window.ym(YM_ID, 'reachGoal', name, params || {}); }
      catch (e) { /* no-op */ }
    };

    // Any data-track element sends its goal on click.
    document.addEventListener('click', function (e) {
      var el = e.target && e.target.closest ? e.target.closest('[data-track]') : null;
      if (el) window.track(el.getAttribute('data-track'));
    }, { passive: true });

    // Load Metrika only after consent.
    var loaded = false;
    function loadMetrika() {
      if (loaded) return; loaded = true;
      (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
      m[i].l=1*new Date();for(var j=0;j<document.scripts.length;j++){if(document.scripts[j].src===r){return;}}
      k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
      (window,document,'script','https://mc.yandex.ru/metrika/tag.js?id=' + YM_ID,'ym');
      window.ym(YM_ID, 'init', { webvisor:true, clickmap:true, accurateTrackBounce:true, trackLinks:true });
    }

    // Consent state.
    var banner = document.getElementById('cookieConsent');
    var consent = null;
    try { consent = localStorage.getItem(KEY); } catch (e) {}

    if (consent === 'granted') {
      loadMetrika();
    } else if (consent !== 'denied' && banner) {
      banner.hidden = false; // No stored choice, show the banner.
    }

    function setConsent(v) {
      try { localStorage.setItem(KEY, v); } catch (e) {}
      if (banner) banner.hidden = true;
      if (v === 'granted') loadMetrika();
    }

    var accept = document.getElementById('ccAccept');
    var decline = document.getElementById('ccDecline');
    if (accept) accept.addEventListener('click', function () { setConsent('granted'); });
    if (decline) decline.addEventListener('click', function () { setConsent('denied'); });
  })();
</script>
{/* noscript-пиксель Метрики намеренно убран: при выключённом JS он грузился
   бы без согласия пользователя, что противоречит consent-based подходу (152-ФЗ). */}
